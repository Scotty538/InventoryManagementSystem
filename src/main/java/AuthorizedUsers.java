public abstract class AuthorizedUsers {
    private static final String[] USERS = {"p1", "p2", "p3", "m1", "m2"};

    private static final String[] PASSWORDS = {"p1", "p2", "p3", "m1", "m2"};

    // Getters and setters
    public String[] getUsers() {
        return USERS;
    }
    public String[] getPasswords() {
        return PASSWORDS;
    }



}
